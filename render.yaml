services:
  - name: minix-gongu-dashboard
    type: web
    runtime: static
    buildCommand: cd review-app && npm ci && npm run build && ls -la ../review-assets
    staticPublishPath: .
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
